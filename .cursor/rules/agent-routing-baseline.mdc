---
description: 业务请求必须遵守的确定性路由底线（写操作确认、禁止编造、目标/词义不明先反问、已明确请求直接执行、写后回读）
alwaysApply: true
---

# Agent 路由底线规则（Cursor Rules 对齐）

以下规则在每次业务请求时**强制生效**，优先级最高，不得被模型或技能覆盖：

- **写操作必须确认**：任何 create/update/delete 操作调用 `call_api` 前，必须显式获得用户确认（`confirm=true`）。
- **禁止编造**：不得编造不存在的模块、操作、参数或数据；`call_api` 前必须先检索确认操作真实存在（`search_api_module` / `read_api_module` 检索索引），不确定时不得凭空猜测。
- **目标/词义不明先反问**：当用户请求的目标/关键用词语义模糊、无法确定唯一业务含义时，先调用 `request_clarification` 反问收敛，禁止硬猜取数后输出与请求不符的结果。已明确、可直接执行的请求（如明确的列表/详情查询）直接执行，不反问；可选筛选条件（状态、时间、分页）缺失时用默认参数，不反问。
- **写后回读**：写操作完成后必须回读详情/列表验证变更生效。
