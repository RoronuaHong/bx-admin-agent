# Agent 文档目录（唯一来源）

本目录是 **bx-admin-agent** 的 Agent 章程与 superpower 配置所在地。  
PC 后台仓库 `bx-film-admin-in2` 不再维护 `docs/agent` 正文（仅保留迁移说明占位）。

| 文件 | 说明 |
|---|---|
| [AGENT_CHARTER.md](./AGENT_CHARTER.md) | 五层模式总章程（含原则1.1 多轮搜索校对） |
| [WORKFLOW_CLARIFICATION_GATE.md](./WORKFLOW_CLARIFICATION_GATE.md) | 澄清门设计 |
| [PC_STRUCTURE_AND_OUTPUT_TYPES.md](./PC_STRUCTURE_AND_OUTPUT_TYPES.md) | PC 结构与输出类型 |
| [CAPABILITY_MATCH.md](./CAPABILITY_MATCH.md) | 能力 vs 需求匹配评估 |
| [field-mapping.json](./field-mapping.json) | 字段映射（normalize_output） |
| [clarification-policy.json](./clarification-policy.json) | 澄清策略 / 四元组 / 项目注册表 |
| [ENTERPRISE_ROADMAP.md](./ENTERPRISE_ROADMAP.md) | 企业级能力路线图（企业内项目 + 知识库 + 智能问法 的差距与待办） |
| [CHAT_FLOW.md](./CHAT_FLOW.md) | 智能问法全场景流程（输入一句话 → 8 大场景完整链路 + 防护点） |
| [VERIFY_SCENARIOS.md](./VERIFY_SCENARIOS.md) | 多场景端到端验证报告（简单/复杂/超复杂/续聊/错误返回，2026-08-22） |
| [PROMPT_ARCHITECTURE.md](./PROMPT_ARCHITECTURE.md) | Prompt 分层架构：阶段1（输入→工具返回）零业务 prompt / 阶段2（工具返回→输出）薄 prompt，含去留决策表与可行性检查 |
| [MULTI_AGENT_ARCHITECTURE.md](./MULTI_AGENT_ARCHITECTURE.md) | Multi-Agent 架构规划（Supervisor 路由 + Worker 配置化上下文；领域 × 环境维度；M0→M3 落地路径，2026-09-02） |
| [A2A_INTEGRATION.md](./A2A_INTEGRATION.md) | A2A 接入方案（Agent2Agent 标准：对外 A2A Server 暴露 + 挂 M3 的 A2A Client 编排；Agent Card/JSON-RPC/Task→chatStream 映射/只读安全，2026-09-03） |

环境变量（`apps/agent-server/.env`）：

- `FIELD_MAPPING_PATH` → 本目录 `field-mapping.json`
- `CLARIFICATION_POLICY_PATH` → 本目录 `clarification-policy.json`

代码默认通过 `apps/agent-server/src/agent-docs.ts` 解析到本目录，无需依赖 `bx-film-admin-in2/docs`。

---

## 流程改进日志

| 日期 | 模块 | 问题要点 | 改了什么 |
|---|---|---|---|
| 2026-08-22 | 修复方法论 | 「优惠活动配置列表 → Parameter checking failed」根因是候选给错模块（配置 key 与脚本查找 key 不一致导致带目录模块别名永不生效） | **AGENT_CHARTER.md 新增「〇、Bug 修复标准流程」**：五步断层定位 + 三不原则 + 服务端护栏补全，任何修改前强制过此流程 |
| 2026-08-22 | 模块定位架构 | 完全抛弃 aliases：手动/自动候选映射在多轮踩坑后暴露「映射表比没有更糟」（漏配→模型误调错接口 400）。终极解法是让候选不存在——模块定位 100% 交模型实时 grep 源码 | **移除**候选 brief/enum（chat.ts、api-index.ts）、parse_intent 关键词表与 project 级合并（tools.ts）、orchestrate 候选补全（workflow-orchestrate.ts）、运行时别名叠加（api-index.ts）、生成脚本 MANUAL_ALIASES（generate-api-index.mjs）、project-aliases.json 的 moduleAliases 段；**新增** orchestrate 兜底 grep 定位（src/api 路径 / views 的 @/api/ import 提取，resolveModuleFromGrep）、extractGrepPattern 剥离动词提业务词（禁止含「搜索」）、inferCallOperation 空 module 拦截。端到端回归：优惠活动配置 12 条 / 影片搜索统计 / 用户列表 全 PASS |
| 2026-08-22 | 多项目 gitlab 代码库（方案 A） | 新项目代码在 gitlab 无本地 clone 时无法 grep 定位；gitlab REST API（http 301→https，443 不通）不可用，但 git 协议（本机 credential manager 凭据）可非交互访问 | **新增**项目注册表（project-registry.ts，读 clarification-policy.json project.options 的 gitRepo/branch/codebaseRoot）、gitlab-repo.ts（git 协议 clone/fetch + checkout -B 到分支，porcelain 保护工作区）、project-context.ts（请求级按 activeProject 解析代码根目录，chat.ts 入口 set）、sync-gitlab-project.mjs（拉取 + 按项目生成 api-module-index-<key>.json）、api-index.ts per-path 缓存按项目加载索引、.env 加 GITLAB_GIT_BASE/GITLAB_REPO_CACHE_DIR。bx-film-admin：测试=dev、生产=master。回归：优惠活动配置/用户列表全 PASS |
| 2026-08-22 | agent 改代码并提交（写工具） | 用户要求 agent 直接改 gitlab 业务项目代码并提交（能力扩展） | **新增工具** `write_code_file`（改/增代码文件，resolveCodebasePath 防越界 + 禁 .git/node_modules/dist；内容 ≤1MB）与 `git_commit_push`（git add -A → commit → push origin <分支>，非交互凭据；master/main 生产分支保护——除非 allowMaster=true 且经二次确认；porcelain 无改动跳过；commit 身份用本机 config 兜底 agent@local）。两者在 chat.ts 工具节点**无条件强制确认**（写代码/提交前弹确认）。mcp.ts 同步注册。push 鉴权已验证（dry-run Everything up-to-date）。测试：合法写/越界拒/.git 拒/node_modules 拒/master 拦截/本地提交+还原 全 PASS |
| 2026-08-22 | 渲染列提取查缺补漏 | 组合修复后回归发现：①用户列表表头泄漏 "${getTran("（模板字符串 title 被首个单引号截断）；②clientType 等英文未中文化（getTran 多行调用带尾部逗号未匹配）；③findConfigFiles 缓存 key 缺 codebaseRoot（多项目切换会命中旧项目列）；④field-mapping 新段缺 _apiFile | **修复**（output-tools.ts）：①新增 `extractTitleFromBlock` 统一提取 4 种 title（反引号模板+后缀拼接/普通字符串/getTran 函数调用（兼容多行+尾部逗号）/i18n t() 查 zh-CN locale）；②getTran 正则允许尾部逗号；③findConfigFiles 缓存 key 加 `${root}::${moduleHint}`（30s TTL）；④field-mapping 补 _apiFile。端到端：优惠活动配置 12 条 + 用户列表表头全中文 PASS |
| 2026-08-24 | 纯 i18n 页面模块定位（A+ 反查） | 「账号合并 5585230699772928」菜单/路由/接口全存在但 agent 报「未找到模块」——中文仅存在于翻译表 tran40.ts，grep 链路两处把 locales 命中丢弃/不识别。过程中又暴露 3 个隐藏坑：①extractGrepPattern 剥离动词后粘连数字（「账号合并558523069977」）导致全 src grep 零命中；②inferCallOperation 列表兜底正则「包含写动词」误排除 getMergeLogs（读日志接口名字含 Merge）；③call_api 兜底强制按 operationType 推断 method，POST 接口被 GET 调用 → 网关「invalid method of HTTP」 | **新增** `src/translation-lookup.ts`（四跳实时反查：术语→翻译表 key→路由 meta.title 引用→组件→@/api/ import→模块 id；零映射表/零静态产物，per-root mtime 缓存，findTranslationKeys 剥离数字/英文/标点的中文变体兜底；workflow-orchestrate.ts 与 tools.ts 共用）。**修复**：①resolveModuleFromGrep 增 term 反查分支（api/views 均未定位成功且 term 非空即反查，唯一候选直接用/多候选诚实提示）；②search_api_module 翻译表命中分支改返回 `[翻译表反查]` 候选清单交模型裁决；③inferCallOperation readOp 排除改为「仅排除以写动词开头」（getMergeLogs 恢复可选中）；④call_api method 优先取索引 operation 真实 method（operationType 推断仅兜底）。**新增** `scripts/verify-translation-lookup.mjs`（5 例断言含数字残渣变体）。端到端：账号合并 1 条 10 列中文表头 / 优惠活动配置 / 用户列表 / 你好闲聊 全 PASS |
| 2026-08-24 | A+ 反查查缺补漏 | 对当日 A+ 改动做全面 review 发现的缺口：①`resolveRouteTitle` getTran 正则 `\s*,?\s*\)` 不匹配多参数形态 `t(getTran('FECB…','[编辑用户]', false, true))`（编辑页 hideMenu title）→ 中文解析不出；②`resolveI18nValue` 只查 zh-CN 根目录翻译文件，key `routes.basic.errorLogList`（zh-CN/routes/ 子目录）解析为空（实测业务菜单无 routes.* 引用，仅 login/errorLogList 全局页受影响，属正确性补全）；③`resolveModuleFromGrep` docstring 第 3 条仍写「仅命中 locales」，与已放宽的「term 非空即反查」不符；④verify 脚本注释「断言 4 例」实为 5 例；⑤`search_api_module` 主路径 rg 只搜 api/views，query 含数字残渣时 grepCodebaseNative 也零命中 → 落到索引兜底死路，而 lookupTermModules 内部能剥离数字变体 → 索引兜底前补反查；⑥`lookupTermModules` 去重逗号表达式改为清晰循环 | **修复**：①getTran 正则改 `[^)]*\)` 兼容多参数；②resolveI18nValue 按 key 中间段补 `zh-CN/routes/<文件>.ts/json` 候选；③docstring 同步；④注释改 5 例并新增「错误日志」诚实回退反向断言（routes/ 子目录系统页 sys/error-log 无 api import → 不产生候选）；⑤tools.ts 索引兜底前补 lookupTermModules 反查；⑥去重循环化。验证：verify 6/6（5 正向 + 1 反向）+ 端到端账号合并 10 列中文表头 PASS |
| 2026-08-23 | 字段映射配置退役 → skill 规范模型 | 用户要求：不要手工维护 field-mapping 的字段/枚举映射，用 skill 规范模型遇英文字段自己去源码找中文映射（多项目下配置表无法隔离，源码+skill 天然按项目） | **新增** `skills/pc-column-mapping/SKILL.md`（规范模型：表头/枚举英文时 read_file/grep_codebase 读当前项目 configs.data.tsx 列 title / useFormSchema options / locale zh-CN / options.ts 位掩码，不猜不编造，normalize_output+render_table 输出）；**渲染钩子**（workflow-orchestrate.ts）：renderListForAgent/renderDetailForAgent 检测英文表头/字段（排除 colTitle 通用字典已覆盖的 id→ID）→ 返回 needsModelMapping → chat.ts 走 [workflow/output-align] 交模型按 skill 补映射；**field-mapping.json 退役**：删所有模块 fieldMap/enumMap/globalEnums（死配置），仅保留 film renderRules（位掩码位值等渲染行为）；read_field_mapping 引导模型用 skill；pc-output-formats 与 workflow 指南同步。端到端：优惠活动配置/用户列表中文表头无回归；needsModelMapping 单元验证 PASS（id(ID) 不误报） |
| 2026-08-24 | 枚举值翻译（源码提取 + skill 值检测） | 用户要求「能中文化的都中文化」：表头已全覆盖，但枚举值（provider=3、clientType=1、type=10）未翻译且无触发链 | **源码枚举提取器**（output-tools.ts）：`extractColumnEnumsFromSource`（customRender 箭头/块体 `return MAP[record.x]` 引用常量表或 import 跨文件跟随、单三目、render 布尔）+ `extractFormOptionsEnums`（useFormSchema options 字面量数组 / `getOptionsForObj(X)` 函数调用 → 跟随 const X）+ `execGetFieldMapping` 工具（源码驱动，替代配置）；**渲染接入**：`loadModuleRenderConfig` 合并源码枚举到 enums（renderCell 自动翻值，30s TTL + root 隔离），renderList/renderDetail 返回 `needsValueMapping`（短数字枚举未翻译 → 交模型 skill，排除 count/total/timeLimit/cycle/days 等数值语义列防误报）；**chat.ts** 列表/详情分支合并 needsValueMapping 走 output-align；**skill pc-column-mapping** 补值翻译规范（读 customRender/options/import 常量翻枚举值 → render_table 传中文值）。验证：special_offer type=10→首充优惠、account provider=3→email（PC 同英文平台名）、clientType 动态 API 映射提取不到 → 主路径触发模型 skill；timeLimit/cycle 不误报 |
| 2026-08-24 | Prompt 分层精简 | 「输入→调用工具返回数据」阶段大段业务工作流指南（llm-first 22 行 + superpower 7 行）与 Function Calling/Cursor 最佳实践（工具 description 承担引导、system prompt 回归人设边界）相悖，且与 resident rules / 工具描述大量重复 | **PROMPT_ARCHITECTURE.md（新建）**：阶段1 零业务 prompt——删 `[workflow/llm-first]` + `[workflow/superpower]`，替换为极简角色 `[workflow/agent]`（第一步 submit_understood_intent → 检索 → call_api，可多轮，中文，不复述内部步骤）；intent-context 缩 1 行、chit-chat 缩 3 行；引导职责逐条下沉工具 description（search_api_module 补「直接读源码勿绕路」）。阶段2 薄 prompt——output-align 5 处提取统一 `outputAlignStep` 模板（pc-column-mapping 指引 + 禁透传英文/枚举），stop/list/detail/validate 保持一行。可行性检查：llm-first/superpower 每一条均有确定性承接者（工具 description / resident rules / 极简角色 / 机制），token 节省约 960 字符 → 170 字符。风险：弱模型不主动调工具 → 首轮 tool_choice=required 机制级兜底 + 极简角色明示 |
| 2026-08-24 | 聊天记录服务端持久化（方案 C） | 聊天记录此前纯前端 localStorage（5MB 上限 + filter 误删 + 身份 key 错位导致刷新丢失）；Cursor 也是本地 SQLite 非云端 | **后端**：新增 `conversations.ts`（MongoDB 单例连接，集合 `bx_agent.chat_conversations`，按 `ownerKey=countryId:loginName` 归属，失败降级内存 Map）；`app.ts` 新增鉴权 REST 接口 `GET/POST /chat/conversations`、`GET/PUT/DELETE /chat/conversations/:id`、`POST .../messages`（upsert）、`POST .../clear`（均 `getSession` 校验）。**前端**：`api.ts` 新增 `fetchConversations/createConversation/saveConversationMessages/deleteConversation/clearConversation`；`ChatPage.vue` 的 `saveConversations` 改为逐会话后端 upsert + localStorage 离线兜底，`restoreConversations` 改 async（先本地渲染 → 服务端权威覆盖），新建/关闭/清空会话同步后端。验证：未登录 401、登录后 list/create/save/read/delete 全 200，Mongo 已落库 |
| 2026-08-24 | 请求入口预处理优化 | ①resident rules 7 条逐条一个 system step 稀释遵循度且多耗 token；②5 个 skill description 纯关键词堆叠不符合 Claude「触发条件」规范；③KB 关键词预检短路（KB_KEYWORDS+forcedReply）与模型自主调 search_knowledge_base 工具功能重复，词表低召回/业务句误短路/无模型整合偏离 RAG 主流 | **①rules 精简 7→4**（agent-routing-baseline.mdc）：删「PC 日志对齐」「展示前 normalize_output」两条已下沉服务端的重复项，「先检索再调用」并入「禁止编造」；chat.ts 合并为 1 个 `[workflow/rules]` step 注入。**②description 规范化**：output-report-chart / write-confirm / pc-output-formats / export-preview / media-bi-richtext 统一为「触发场景句+触发词」。**③删除 KB 预检短路**：chat.ts 移除 KB_KEYWORDS 正则+searchKnowledgeBase+[knowledge-base] 注入+forcedReply（保留 forcedReply 注解供列表/详情/报表受控渲染用），import 清理；chit-chat 分支补「知识库文档类问题可调 search_knowledge_base」提示；检索实现（TF-IDF+embedding RRF）与工具注册不动。**skills 预算护栏**：skills.ts 加载超 16000 字符告警 + chat.ts 注入端截断。文档同步：PROMPT_ARCHITECTURE/CHAT_FLOW/ENTERPRISE_ROADMAP/chat-flow-diagram |
| 2026-08-24 | 模型工具调用提取器 + 列渲染 + 环境（业务查询全链路修复） | zen 免费链模型不走 function calling 通道，工具调用以纯文本 `[[{name,parameters}]]`（裸嵌套数组、常残缺未闭合）输出；原状态机只认 `{tool_calls:[...]}` 对象 + 完整 JSON.parse → 静默提取失败 → 整轮 toolCalls=0 → 账号合并/优惠活动配置/用户列表全部调不起工具（表现为「业务查询全错」）。另：`pickRowsByPcColumns` 严格 `if (k in sample)` 过滤 PC 列，点路径 dataIndex（`userVipInfoRes.vipType`/`userVipInfoRes.endDate`）永远不匹配扁平 row key → 用户列表只渲染 4 列（缺客户端类型/渠道/会员等级/注册时间/会员到期/最后登录）；`isToolPlanText` 不识别裸数组/嵌套数组 JSON → 提取失败时残留 JSON 上屏；rg 不在 PATH + node_modules 无 ripgrep 包 → grepCodebaseNative 全量递归回退（每轮刷 3-4 次 'rg' is not recognized + 分钟级慢） | **models.ts（A 层决定性修复）**：`tryExtractToolCalls` → `extractToolCallsFromJson`（对象/裸数组/嵌套数组/单对象 4 形态）+ `extractToolCallsViaRegex` 括号配平兜底（不依赖 JSON 整体闭合，兼容 parameters 内嵌套对象）；状态机进入条件 `/^\s*\{/` → `/^\s*[\{\[]/`（主循环 + 尾部 buffer 4 处）。verify-text-toolcall.mjs 6/6（残缺/完整/对象/input 键/普通文本/普通 JSON）。**workflow-orchestrate.ts（B 层）**：pickRowsByPcColumns 无条件保留所有 PC 列（缺数据时 renderCell 优雅降级空字符串）+ renderCell 加点路径嵌套逐层取值；execGetListColumns({module:"user"}) 实测返回 account/user 10 列（score=4 最高）。**chat.ts（C 层）**：isToolPlanText 补 `/^\s*[\{\[]/` + 工具名白名单 + `parameters|input` 键识别。**环境**：winget 首次 msstore 源证书错误（0x8a15005e）→ `--source winget` 绕过安装 ripgrep 15.2.0（C:\Users\Administrator\AppData\Local\Microsoft\WinGet\Links\rg.exe），pm2 重启 agent-server-dev 继承新 PATH。端到端：用户列表/账号合并/优惠活动配置全 PASS |
| 2026-08-25 | 对齐 Cursor 反问机制（歧义反问落地） | 「二级分类模块的链接」输出不对：模型把「链接」误解成 API 接口地址（输出 /v0.1/category/getCategoryType）、把「二级分类」错定位成 category/categoryType（正确=country），且 0 次反问。5 免费模型 schema 实测：多数能理解「链接」=路由/接口（zenhy3/xpreviewfree/lagunas），但**都不反问**（「自信误判不觉得模糊」+ 行动取数据）。对照 Cursor 46.11 泄露提示词：AskQuestion 描述纯正向（无「查询别反问」）、`<tool_calling>` 有「缺参/目标不明 ask the user」、**无「查询不反问可选条件」这类写死压制**——本仓库的 `request_clarification` 描述「查询类即使缺筛选条件也不反问」是独有的过度压制源头 | **tools.ts** request_clarification 描述去掉「查询类缺筛选条件不反问」负面句，改为 Cursor AskQuestion 式纯正向「目标/词义不明用结构化问题反问收敛」；**.cursor/rules/agent-routing-baseline.mdc** 把「查询不反问可选条件」改为「目标/词义不明先反问，已明确请求直接执行、可选筛选条件缺失用默认参数」；**chat.ts** `[workflow/tool-calling]` 补第 4 条「目标/词义不明先反问」绿灯条款 + resident rules 注释同步；**PROMPT_ARCHITECTURE.md / AGENT_CHARTER.md / README** 同步。全为 prompt 引导、无词形写死、判定交模型。验证（nemotronultra 重启后）：「二级分类模块的链接」178.9s → 模型 summary 写「接口链接/菜单链接」、输出末尾主动提及「前端菜单路由路径」并反问「需要我进一步查看前端菜单路由配置吗」（对比修复前完全硬猜、无路由维度），module 仍错定位 categoryType（模型能力边界）；回归「用户列表前3页」123.9s /「影片列表前2页」32.4s 均直接取数无过度反问。注意：loadResidentRules 进程内缓存，改 .mdc 需 pm2 delete+start agent-server-dev 生效 |
    